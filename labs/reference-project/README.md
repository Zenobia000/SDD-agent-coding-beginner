# 參照專案 — SmartTrip FX

老師跑完 [`BUILD.md`](../../BUILD.md) 的產出物。**比對結構，不要抄內容。**

| 檔案 | 對應 BUILD 第幾步 | 比對什麼 |
|---|---|---|
| [`01-decision-card.md`](./01-decision-card.md) | 1 框題目 | 欄位的具體程度（現況有沒有數字、判準能不能 yes/no） |
| [`02-PRD.md`](./02-PRD.md) | 2 寫 PRD | Constraints 裡有沒有混進「怕它出錯」的叮嚀 |
| [`03-split.md`](./03-split.md) ⭐ | 3 畫那條線 | **錢的計算有沒有落在 AI 那半** |
| [`04-evals.md`](./04-evals.md) | 5 建考卷 | 判準能不能用程式判、有沒有刻意會 fail 的案例 |

---

## 這個題目是什麼

出國前算「該換多少現金」。

換太多 → 回國再換虧匯差；換太少 → 現場狼狽。
一趟 NT$40,000 的旅程，這兩件事合計會吃掉 **NT$300–1,200**。

現有的行程規劃工具完全不處理這件事，旅客只能憑感覺換。

---

## 為什麼用它當教材

因為它**天然切成兩半**，而那條切線就是這套方法的核心：

```
AI 判斷（不可驗證 → schema + 考卷）
    · 排行程
    · 標每一項是現金還是刷卡
─────────── 交界處：schema + 一個 if ───────────
程式計算（可驗證 → 單元測試）
    · 加總 × 預備金 → 建議換匯額
    · 今日匯率 vs MA30 → 燈號
    · 幣別換算、預算檢查
```

多數學員的第一直覺是「反正都要叫 AI 產行程了，順便叫它算總額」。
**那會得到一個算術偶爾出錯、而且錯了也不知道的系統。**

[`03-split.md`](./03-split.md) 專門講這件事。

---

## 這裡沒有 code

刻意的。

`BUILD.md` 的每一步都是**你和 CLI 討論出來的**——PRD、架構、schema、測試，
全部在對話裡長出來。放一份寫好的 code 在這裡，你會直接抄，
然後跳過那個真正該學的判斷過程。

**留下來的四份是「判斷的產物」，不是「實作的產物」。**

可以直接複製的實作在 [`../blocks/`](../blocks/)（5 塊，47 個測試）。

---

## 給講師

開課前自己跑一遍 `BUILD.md`，用**你自己的題目**，並把產出覆蓋到這四份。

理由：學員會問「老師你的長什麼樣」，而用 SmartTrip FX 示範他們會照抄。
