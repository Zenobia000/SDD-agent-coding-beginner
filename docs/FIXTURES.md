# Fixture 影片挑選準則

> 給講師的準備清單。學生不需要讀這一份。
>
> 目標：**用最少的影片，涵蓋最多的失敗模式**，並且讓每一關的驗收訊號可以寫死成一條命令。

---

## 為什麼要有固定 fixture

課堂上讓學生自由挑影片有兩個問題：

1. **驗收訊號無法寫死。**「這支影片必須抽出 7 樣食材」這種可 pass/fail 的檢查，只有在影片固定時才成立。沒有它，第一冊的不變量 1 在第二冊就落不了地。
2. **網路掛掉整堂課停擺。** 抽幀與轉錄都吃頻寬，教室網路撐不住三十個人同時下載。

所以採兩層：

| | 用途 | 誰準備 |
|---|---|---|
| **固定 fixture（6 支）** | 寫死的驗收訊號、離線可跑 | 講師預先下載進 repo |
| **學生自選** | 撞 fixture 沒涵蓋的地雷 | 學生自己挑頻道 |

第二層才是真正的教學現場 —— 學生會挑到方言、純手寫字幕、沒有字卡的影片，然後發現自己的實作垮掉。那正是不變量 6（卡住就縮小範圍）的練習。

---

## 六支 fixture 必須涵蓋的特徵

每一支對應一個具體的失敗模式。挑選時**以特徵為準，不以菜色為準**。

| # | 代號 | 必須具備的特徵 | 它在教什麼 |
|---|---|---|---|
| 1 | `baseline` | 人工字幕完整、單一道菜、5–8 分鐘 | 快樂路徑。第一張票的綠燈就靠它 |
| 2 | `caption-only` | **份量不出現在語音裡** | 為什麼純 STT 不夠 —— 多模態的存在理由 |
| 3 | `vague` | 大量「適量」「少許」 | 需求歧義。schema 要不要允許 null |
| 4 | `no-caption` | **完全沒有字幕**（人工與自動都沒有） | 上游資料不可信。要不要自己跑 STT？成本與品質的取捨 |
| 5 | `long` | 超過 40 分鐘，含大量閒聊 | 成本與切段策略。整支丟進模型會爆 token 也會爆錢 |
| 6 | `multi-dish` | 一支影片做多道菜 | 「一支影片 = 一張食譜卡」這個假設會壞掉 |

第 6 支是刻意設計的地雷：**學生的第一版 schema 幾乎一定是「一支影片對一張食譜」**，撞到這支才會發現要拆。這是不變量 4（可逆性）最好的教學時刻 —— 那時他已經存了幾十筆資料，得決定要不要寫遷移。

---

## 挑選時的硬性條件

- **中文發音**。這門課的讀者是中文使用者，英文影片會讓 STT 的難度失真。
- **公開、非會員限定**，且可用 `yt-dlp` 取得。
- **避開有明顯背景音樂蓋過人聲的**，那會讓第 4 支以外的影片都變成 STT 難題，混淆變因。
- **長度分佈**：1–5 支控制在 5–10 分鐘，第 6 支可長。整批下載後總量建議 < 500 MB。

---

## 存放方式

```text
fixtures/
├── README.md              每支的代號、來源 URL、涵蓋特徵、預期抽取結果
├── baseline/
│   ├── source.txt         URL 與下載日期
│   ├── audio.m4a
│   ├── captions.vtt
│   ├── frames/            已抽好的關鍵幀
│   └── expected.json      ← 驗收訊號的來源
└── ...
```

`expected.json` 是整份 fixture 的核心。它不是標準答案，是**下限**：

```json
{
  "must_extract_ingredients": ["蒜", "醬油", "米酒"],
  "must_extract_count_min": 7,
  "must_have_quantity_for": ["醬油", "米酒"],
  "known_ambiguous": ["蔥花（適量）"],
  "must_not_hallucinate": ["奶油", "起司"]
}
```

`must_not_hallucinate` 那一欄特別重要 —— 它抓的是模型「自己加料」的行為，而這是多模態任務最常見也最難發現的錯誤。

---

## 影片會消失，這是已知風險

YouTube 影片會被下架、改成私人、或關閉字幕。所以：

- `source.txt` 記下**下載日期**與影片標題，方便日後找替代
- 抽好的 audio / captions / frames **進版控**（或至少進 Git LFS），不要每次上課重抓
- 版權上僅作課堂教學的最小重現使用，不重新發布完整影片

要換掉某一支時，**以特徵為準去找替代**，不要找「類似的菜」。特徵表才是這份 fixture 的規格。

---

## 已挑選的六支（2026-08-13）

中英各三。**每一條都標了證據等級** —— 我用 `yt-dlp` 實際抓過 metadata 與字幕，但 YouTube 會對頻繁的字幕請求限流，所以有些只驗到結構，沒驗到內容。

| # | 代號 | 影片 | 長度 | 語言 | 驗證到什麼 |
|---|---|---|---|---|---|
| 1 | `baseline` | [One-pan orange chicken](https://www.youtube.com/watch?v=zc63e7ZKUrg) · Adam Ragusea | 6 分 | EN | 【結構已驗】人工字幕 1 種、自動 7 種、單一道菜。<br>⚠️ 字幕內容未驗（遭限流）；「該頻道份量精確」屬**推論**不是驗證 |
| 2 | `caption-only` | [Mix and Match Meal Prep](https://www.youtube.com/watch?v=qb4JbUjr5tM) · Fit Men Cook | 8 分 | EN | 【內容已驗】只有自動字幕；**實測 1863 字中 0 個份量訊號**。<br>⚠️ 份量是否出現在畫面字卡上**未驗證**（需看影格） |
| 3 | `vague` | [口袋豆腐](https://www.youtube.com/watch?v=u6XG4S0PFyU) · Chef Wang 王剛 | 5 分 | 中（含中英字幕） | 【內容已驗】人工字幕 3 種；**實測 1134 字、12 個模糊量詞、13 個份量訊號** |
| 4 | `no-caption` | [麻婆豆腐](https://www.youtube.com/watch?v=xd6iraGmgq0) · 阿慶師 | 10 分 | 中 | 【結構已驗】**人工 0、自動 0 —— 完全沒有字幕**。單一道菜 |
| 5 | `long` | [1 Hour of Martha Stewart](https://www.youtube.com/watch?v=WD1ErFqR46A) | 73 分 | EN | 【結構已驗】73 分鐘、只有自動字幕。<br>⚠️ 字幕內容未驗（遭限流） |
| 6 | `multi-dish` | [電鍋一鍵按下 3 道菜](https://www.youtube.com/watch?v=iEAZ4BwKXpo) · Apple loves cooking | 9 分 | 中 | 【內容已驗】人工字幕 3 種；**實測 1798 字、11 個份量訊號、1 個模糊量詞**；標題明示 3 道菜 |

### 挑選時排除掉的一個陷阱

楊桃美食網有一支「8 道必點館子菜」（`HhNHZ2v4Uk8`，4 分，完全無字幕），一度是 `no-caption` 的候選。

**但它同時是「無字幕」和「多道菜」** —— 兩個變數混在一起，學生撞牆時分不清是哪個特徵造成的。fixture 的價值在隔離單一變數，所以換成阿慶師那支單一道菜的。

### 開課前你還是要自己看過一遍

我能驗的是「影片存在、多長、有沒有字幕、字幕裡有沒有份量與模糊量詞」。**我沒辦法看影片畫面**，所以這兩件事需要你確認：

1. `caption-only` 那支的份量**真的**打在畫面字卡上（而不是根本沒講份量）
2. 每一支的 `expected.json` 內容 —— 必要食材、最少數量、不得出現的幻覺項

### 重新驗證或換片

```bash
bash scripts/probe-fixture.sh <video_id>
```

會印出長度、人工／自動字幕數、字幕字數、份量訊號數、模糊量詞數。
**字幕字數突然變 0 通常是 YouTube 限流，不是影片變了** —— 隔一陣子再跑。

---

## 尚未決定

| 項目 | 狀態 |
|---|---|
| 每支的 `expected.json` 內容 | ⬜ 需要人看過影片才能填 |
| audio/frames 進 Git 還是 Git LFS | ⬜ 看實際體積再決定 |
| 是否提供已轉錄好的文字，讓不想跑 STT 的學生也能繼續 | ⬜ 待決定 |
