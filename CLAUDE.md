# CLAUDE.md

這個 repo 有兩個身份：一套可以直接拿去用的 Claude Code 設定，以及教這套設定怎麼來的課程。

你在這裡開工，多半是為了第一個身份——**做出一個東西**。

---

## 這裡沒有必經的關卡

`.claude/skills/` 是**參考書，不是流程圖**。八個技能可以任何順序取用，也可以完全不用。

不知道現在該做什麼的時候打 `/next`，它會看你的專案現況給一個建議——**建議，不是指令**。

不要在使用者沒問的時候一直推薦 skill。他在心流裡的時候，最沒用的東西就是一個提醒他該走流程的助理。

---

## 一件貫穿的事

改東西之前，先講清楚三件事：**這輪動哪些檔案、什麼算變好、幾輪後停**。

這不是規定，是經驗。這三件事沒講清楚的時候，改到第五版你會分不清哪版比較好，也不知道什麼時候該收手。

第二件最常被跳過，也最貴——「感覺比較好」不可重現，隔天問你會得到不同答案。真的講不出判準時 `evals` 有一套拆解方法。

---

## 講話

繁體中文。技術術語直接用英文。

**結論先行。** 被問「該選哪個」時用推薦的選擇開頭，不要用一個動作開頭。

**證據要分級。** 已確認的附路徑行號，推測的寫出依據，未知的寫出查證方式。不要為了簡潔把推測講成已確認——短而武斷比長而誠實危險得多。理由見 `.claude/rules/evidence.md`。

**每次回覆結尾給一個下一步。** 一個，不是三個。

---

## 幾件不必問的事

寫 code 的常識交給你的本能：讀檔前先看過、跑破壞性指令前先確認、命名要說得出意圖、錯誤不要靜默吞掉、function 短一點、不要為想像中的未來需求加抽象。

這些不寫進規則，因為你本來就會。

---

## 幾件不能賭的事

這些由 hook 與 git hook 機械強制，不靠自律：

遞迴刪除、force push、`reset --hard`、在保護分支 commit、把 secret 寫進檔案。

寫進文件的規則大約有七成順從率。不可逆的操作不能賭剩下那三成——所以它們在 `.claude/hooks/` 而不在這裡。

**這是這套設定唯一「你沒得選」的部分。其餘全部是建議。**

---

## 卡住的時候

同一個問題改三輪還沒好，停下來——通常是一開始的假設就錯了，繼續改只會越陷越深。做法見 `.claude/rules/when-stuck.md`。

---

## 這個 repo 的其他部分

`BUILD.md` 是帶人做完一個真實專案的引導文件——使用者第一次來多半是為了它。
`labs/reference-project/` 是那份文件的參考答案，`labs/blocks/` 是可複製的積木，
`docs/` 是讀本，`curriculum/` 是開課用的講師手冊。

改教材時另有一套寫作規約，在 `curriculum/.claude/DOC-CONTRACT.md`。**寫 code 時不用理它。**
